Android Music Player

Project location /home/jay/Documents/Scripts/AI/OpenCode/Android-Apps/J-Net-Android-Music-Player/

Github link https://github.com/jnetai-clawbot/Music-Player


Issue(s):

Add settings so i can choose play options (add all you can think of with a search settings part at the top to find a setting easy)

Add a option (called Crossfade thats on be default) that crossfades between tracks (no silence just fade to next track and never be silent)

Add Auto repeat mode where a playlist plays again from the styart at the end of last track in the current playlist

Add in settings search exclude and search include location (paths) so things that are not music dont get added for example.

Add setting to find mp3 (enabled by default)
Add setting to find music tracks (mp3 as defined in settings) of a minimum lengh for example at least 1 minute long.

Add all other useful player options you can think of.

Add Scan tab and Scan button to find new music (not already in the music database) that uses settings oiptions for inclusion and exclusion, (if no oaths are included or excluded scan the whole phone for music longer than 1 minute (mp3)

Fix About section as nothing is displayed in About, it should have Made By jnetai.com, a version (github release version in full tag, a share button, to share the latest release, a Check for update button (that if available offers to open latest release on github)

Build on github and release as a new update using same keystore so app can update without uninstalling first.

Tell me when done and ready to test.

